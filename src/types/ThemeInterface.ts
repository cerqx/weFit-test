export interface Theme {
  colors: {
    primary: {
      main: string;
    }

    gray: {
      lighter: string;
      light: string;
      main: string;
      dark: string;
    }

    blue: {
      main: string;
      dark: string;
    }
    basic: {
      dark: string;
      white: string;
    }
    green: string;
  };
}