export interface Theme {
    colors: {
      primary: {
        main: string;
      }

      gray: {
        light: string;
        main: string;
      }

      blue: {
        main: string;
        dark: string;
      }
      basic: {
        dark: string;
        white: string;
      }
      green: string;
    };
  }